// AUTO-GENERATED BY nest-drizzle-api-kit. DO NOT EDIT.
import {
  Controller,
  Get,
  Post,
  Patch,
  Delete,
  Body,
  Param,
  Query,
  HttpCode,
  ParseIntPipe,
} from '@nestjs/common';
import { ApiOperation, ApiTags } from '@nestjs/swagger';
import { TabelaCustasService } from './tabela-custas.service';
import { FindTabelaCustasQueryDto } from './dto/find-tabela-custas-query.dto';
import { CreateTabelaCustaDto } from './dto/create-tabela-custa.dto';
import { UpdateTabelaCustaDto } from './dto/update-tabela-custa.dto';

@ApiTags('TabelaCustas')
@Controller('tabela-custas')
export class TabelaCustasController {
  constructor(private readonly service: TabelaCustasService) {}

  @Get()
  @ApiOperation({
    operationId: 'findTabelaCustas',
    summary: 'Find tabelaCustas',
  })
  async find(@Query() query: FindTabelaCustasQueryDto) {
    return this.service.find(query);
  }

  @Get(':id')
  @ApiOperation({ operationId: 'findTabelaCusta', summary: 'Find tabelaCusta' })
  async findOne(
    @Param('id', ParseIntPipe) id: number,
    @Query() query: FindTabelaCustasQueryDto,
  ) {
    return this.service.findOne(id, query);
  }

  @Post()
  @ApiOperation({
    operationId: 'createTabelaCusta',
    summary: 'Create tabelaCusta',
  })
  async create(@Body() body: CreateTabelaCustaDto) {
    return this.service.create(body);
  }

  @Patch(':id')
  @ApiOperation({
    operationId: 'updateTabelaCusta',
    summary: 'Update tabelaCusta',
  })
  async update(
    @Param('id', ParseIntPipe) id: number,
    @Body() body: UpdateTabelaCustaDto,
  ) {
    return this.service.update(id, body);
  }

  @Delete(':id')
  @HttpCode(204)
  @ApiOperation({
    operationId: 'deleteTabelaCusta',
    summary: 'Delete tabelaCusta',
  })
  async delete(@Param('id', ParseIntPipe) id: number) {
    await this.service.delete(id);
  }
}
